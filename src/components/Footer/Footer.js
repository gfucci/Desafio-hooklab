import styles from './Footer.module.css'

const Footer = () => {
  return (
    <footer className={styles.footer}>
        <h2>Gerencie seus produtos</h2>
        <p>React Products &copy;</p>
    </footer>
  )
}

export default Footer